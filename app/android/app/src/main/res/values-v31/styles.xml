<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12 and up. The platform owns the launch screen from here, so
         windowBackground alone is not enough: without these two the launcher
         icon is drawn over it. See docs/PHASE-1.md D4. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_splash_icon</item>
    </style>
</resources>
