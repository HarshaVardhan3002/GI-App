<?xml version="1.0" encoding="utf-8"?>
<!-- The launch window. One flat colour, theme-aware through @color/launch_background. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />

    <!-- Commented out, not deleted: the launch mark was the upstream author's
         unicorn, which is somebody else's identity. GI Daily draws nothing here
         at all - see docs/PHASE-1.md D4. The asset stays on disk.
    <item android:gravity="center" android:drawable="@drawable/ic_launch_image" />
    -->
</layer-list>
