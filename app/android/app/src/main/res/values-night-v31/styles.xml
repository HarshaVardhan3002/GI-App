<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12 and up, dark. Same override as values-v31, with the dark parent
         so the window behind the splash matches values-night/styles.xml.
         See docs/PHASE-1.md D4. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_splash_icon</item>
    </style>
</resources>
